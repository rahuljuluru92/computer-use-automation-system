<%/* Permission denial. Like "not found", this is information rather than
     breakage - but unlike "not found" it may mean a human needs to intervene,
     which is why the replay engine can treat it either as a business outcome or
     as an escalation trigger depending on which step hit it. */%>
<table class="panel" width="620" cellpadding="0" cellspacing="0">
  <tr><th>Access Restricted</th></tr>
  <tr><td style="padding:10px">
    <div class="err" role="alert">
      <b>Permission denied.</b><br>
      Your operator profile is not authorised to service this member record.
      Contact a supervisor for an override. (Policy: SVC-RESTRICT-04)
    </div>
  </td></tr>
</table>
